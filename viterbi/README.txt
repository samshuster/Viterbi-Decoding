must have python 2.7 and numpy
actual viterbi code is in viterbi_impl

To run, run the run_viterbi.py as the main file.

Other data files can be specified (as long as they have same format as the given data) as well as training files by changing
the 

DFile = 'train-data'
TestFile = 'test-data-1.sent'

at top of run_viterbi